import {{camelCase name}} from '../{{camelCase name}}';
import {
  INITIALIZE_{{constantCase name}},
} from '../../actions/{{camelCase name}}';

describe('{{camelCase name}} reducer', () => {
  let initialState;
  beforeEach(() => {
    initialState = {
      /* add state */
    };
  });
  afterEach(() => {
    {{camelCase name}}(initialState, { type: INITIALIZE_{{constantCase name}} });
  });

  test('INITIALIZE_', () => {
    const action = {
      type: INITIALIZE_{{constantCase name}}
    };
    const expected = { ...initialState };
    expect({{camelCase name}}(initialState, action)).toEqual(expected);
  });
});
