import React, { useState, useEffect } from 'react';
import { shallowEqual, useDispatch, useSelector } from 'react-redux';
import styled from 'styled-components';

import {{pascalCase name}}Component from '../component/{{camelCase name}}Component';
import { initialize{{pascalCase name}} } from '../actions/{{camelCase name}}';

const Container = styled.div`
  display: flex;
`;

const {{pascalCase name}} = () => {
  const [isSample, handleSample] = useState(false);
  const allStates = useSelector(state => ({ ...state }), shallowEqual);
  const dispatch = useDispatch();

  useEffect(() => {
    handleSample(true);
    console.log('hello!', allStates);
  }, [isSample]);

  useEffect(() => {
    console.log('running action creator');
    dispatch(initialize{{pascalCase name}}())
  }, []);

  return (
    <Container>
      {isSample ? <{{pascalCase name}}Component /> : 'hello'}
    </Container>
  );
};

export default {{pascalCase name}};
