import React, { memo } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	display: flex;
`;

const {{camelCase name}}Component = memo(() => (
	<Wrapper>
		{{camelCase name}} Sample
	</Wrapper>
));

export default {{camelCase name}}Component;
