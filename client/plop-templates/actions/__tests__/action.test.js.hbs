import thunk from 'redux-thunk';
import configureStore from 'redux-mock-store';
import {
  initialize{{pascalCase name}},
  INITIALIZE_{{constantCase name}}
} from '../{{camelCase name}}';

const mockStore = configureStore([thunk]);

describe('{{camelCase name}} action test', () => {
  let store;
  const initialState = {
    /* add state */
  };
  beforeEach(() => {
    store = mockStore(initialState);
  });
  afterEach(() => {
    store.dispatch(initialize{{pascalCase name}}());
  });
  test('initialize{{pascalCase name}}', () => {
    store.dispatch(initialize{{pascalCase name}}());
    const actionTypes = store.getActions().map(action => action.type);
    expect(actionTypes.length).toEqual(1);
    expect(actionTypes).toContain(INITIALIZE_{{constantCase name}});
  });
});